LLM_BENCHMARK_DATA_DIR=data
LLM_BENCHMARK_REPORTS_DIR=reports
# EvalScope SandboxService 是进程级单例：并发智力评测任务会互相 shutdown 共享沙箱，
# 导致代码类数据集（mbpp/humaneval 等）批量执行失败。默认串行执行（并发=1）。
# scripts/start_main.* 已默认导出该值；此处仅为 .env 用户参考。
LLM_BENCHMARK_EVALSCOPE_JOB_MAX_CONCURRENCY=1
